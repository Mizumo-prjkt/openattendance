// OpenAttendance Mobile Setup UI
// Using Framework7

// import Framework7 from "framework7";

// Init 

var app = new Framework7({
    root: '#app',
    id: 'org.mizproject.openattendance',
    name: 'OpenAttendance Mobile Setup',
    panel: {
        swipe: true,
    },
    routes: [
        {

        },
    ],
})

var mainView = app.views.create('.view-main');